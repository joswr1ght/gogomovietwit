Dynamic duration of marquee based on content-length
Add on-screen VLC controls as well.
Fix line breaking
  -- Still broken. I manually add newlines for wrapping, but VLC still treats
     the entire text (including my newlines) for wrapping evaluation. My
     newline wrapping fixes content lost on right edge of screen, but VLC
     then puts the next word on its own line.
